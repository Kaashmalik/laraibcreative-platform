# ==========================================
# NPM CONFIGURATION
# ==========================================
# This file configures npm behavior for the project
# ==========================================

# Use legacy peer deps to resolve conflicts
# Required because multer-storage-cloudinary@4.0.0 requires cloudinary@^1.x
# but we use cloudinary@^2.x (which is compatible but not recognized by peer deps)
legacy-peer-deps=true

# Save exact versions (optional - helps with consistency)
save-exact=false

